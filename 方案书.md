# DiskSense 便携式 AI 磁盘文件管理器

## 完整方案书 v3.3（正式封版交付件）


# 目录

1. [项目定位与设计哲学](#1-项目定位与设计哲学)
2. [系统总体架构](#2-系统总体架构)
3. [技术选型与运行依赖](#3-技术选型与运行依赖)
4. [通信协议选型与设计（核心决策）](#4-通信协议选型与设计核心决策)
5. [便携启动与生命周期管理](#5-便携启动与生命周期管理)
6. [极速扫描引擎（权限分层方案）](#6-极速扫描引擎权限分层方案)
7. [指纹聚合器（Token 优化的核心）](#7-指纹聚合器token-优化的核心)
8. [语义信号生成（非硬编码规则引擎）](#8-语义信号生成非硬编码规则引擎)
9. [实时仪表盘与可视化交互](#9-实时仪表盘与可视化交互)
10. [Agent Skill 完整定义（大脑皮层）](#10-agent-skill-完整定义大脑皮层)
11. [本地服务 API 契约（通信协议的具体实现）](#11-本地服务-api-契约通信协议的具体实现)
12. [文件操作与持久化回溯引擎](#12-文件操作与持久化回溯引擎)
13. [用户偏好与长期记忆系统](#13-用户偏好与长期记忆系统)
14. [系统资源治理与性能优化](#14-系统资源治理与性能优化)
15. [隐私与安全合规声明](#15-隐私与安全合规声明)
16. [部署结构与交付清单](#16-部署结构与交付清单)
17. [完整用户旅程（端到端闭环）](#17-完整用户旅程端到端闭环)
18. [验收标准](#18-验收标准)
19. [自主审查与改进记录](#19-自主审查与改进记录)


## 1. 项目定位与设计哲学

### 1.1 产品定义

DiskSense 是一个**由 LLM Agent 驱动的本地磁盘语义化管理工具**。它不是独立的可执行程序，而是一个 **Agent Skill 包**——包含提示词定义（SKILL.md）和 Python 执行脚本。Agent 加载此 Skill 后，获得调用本地 Python 脚本分析磁盘、操控文件、生成可视化报告的能力。

其本质是一个融合了 **Everything 的检索速度 + SpaceSniffer 的可视化 + 文件管理器的操作能力 + ChatGPT 的语义推理** 的便携式 AI 盘符管家。

### 1.2 三大不可违背的铁律

| 铁律 | 详细说明 |
| :--- | :--- |
| **绝对隐私（零内容读取）** | Python 脚本**永久禁止**使用 `open(file, 'r')` 或任何文本解析库读取文件正文。唯一例外是读取文件头 16 字节（Magic Number）用于格式校验，且绝不记录不上传。Agent 全程只接触路径字符串、大小、时间和信号标记，**永远看不到文件里的文字内容**。 |
| **纯便携无痕** | 零注册表写入，零系统服务安装，零开机自启。所有配置、缓存、日志均存放于 Skill 目录下的 `Data/` 文件夹。删除整个文件夹即完成卸载。 |
| **全操作可回溯** | 删除操作强制走 Windows 回收站（`IFileOperation` + `FOF_ALLOWUNDO`）。所有移动、重命名、删除操作均写入持久化 SQLite 日志，支持“一键回滚”和时间线回溯。 |


## 2. 系统总体架构

### 2.1 双子星协作模式

| 实体 | 形态 | 职责 |
| :--- | :--- | :--- |
| **Agent 推理层** | `SKILL.md` + `scripts/api_client.py` + `scripts/launcher.py` | 理解用户语义、调用脚本工具、分析指纹档案、生成清理建议、发送可视化指令 |
| **本地核心引擎** | Python 模块（`disk_sense/server.py`、`scanner.py`、`aggregator.py` 等） | 扫描磁盘、聚合指纹、执行文件操作、管理 SQLite、提供 HTTP/WebSocket 服务 |

### 2.2 架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                        用户交互层                               │
│  ┌─────────────────────────┐    ┌────────────────────────────┐  │
│  │   Agent 聊天窗口        │    │  仪表盘 (HTML + D3.js)     │  │
│  │   (自然语言指令)        │    │  (Treemap + 叠加层)        │  │
│  └────────────┬────────────┘    └───────────────┬────────────┘  │
└───────────────┼─────────────────────────────────┼───────────────┘
                │                                 │
                │ (Python 脚本执行)               │ (HTTP/WebSocket)
                ▼                                 ▼
┌──────────────────────────────────────────────────────────────────┐
│              Agent 推理层 (LLM + Function Call)                │
│  加载 SKILL.md · 执行 api_client.py · 解析 stdout 获取结果     │
│  通信方式：脚本调用（Agent → api_client.py → HTTP → 后端）     │
└───────────────────────────────────┬──────────────────────────────┘
                                    │ (HTTP 请求)
                                    ▼
┌──────────────────────────────────────────────────────────────────┐
│         本地核心引擎 (Python 进程 - 单例)                       │
│  启动 server.py → 监听 localhost:58901                         │
│  同时提供：HTTP API（Agent 调用）+ WebSocket（仪表盘推送）      │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────────────┐  │
│  │  MFT扫描器 │  │ 指纹聚合器 │  │  FastAPI 服务            │  │
│  │(权限分层)  │  │(Entity合并)│  │  (asyncio.to_thread)     │  │
│  └────────────┘  └────────────┘  └──────────────────────────┘  │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────────────┐  │
│  │ 规则引擎   │  │ 文件操作器 │  │  SQLite 回滚管理器       │  │
│  │(结构化条件)│  │(回收站映射)│  │  (五步预检 + 日志)       │  │
│  └────────────┘  └────────────┘  └──────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 2.3 依赖关系图

```
Agent 平台 (Cline / OpenAI Assistant / 自定义)
    │
    ├── 加载 ──────► SKILL.md (系统提示词 + 工具定义)
    │
    └── 执行 ──────► scripts/api_client.py
                        │
                        ├── 调用 ──► launcher.py (启动/停止服务)
                        │
                        └── HTTP ──► disk_sense/server.py (FastAPI)
                                        │
                                        ├── scanner.py (MFT/多线程扫描)
                                        ├── aggregator.py (指纹聚合)
                                        ├── rules_engine.py (信号评估)
                                        ├── file_operator.py (回收站操作)
                                        ├── undo_manager.py (SQLite回滚)
                                        ├── preferences.py (用户偏好)
                                        └── templates/template.html (仪表盘)
```


## 3. 技术选型与运行依赖

### 3.1 核心技术栈

| 组件 | 技术选型 | 版本要求 | 说明 |
| :--- | :--- | :--- | :--- |
| 后端语言 | Python | 3.10+ | 跨平台兼容，丰富的系统 API 封装 |
| Web 框架 | FastAPI | >=0.100.0 | 同时支持 HTTP + WebSocket，自动生成 API 文档 |
| ASGI 服务器 | Uvicorn | >=0.23.0 | FastAPI 的异步服务器 |
| 实时通信 | FastAPI WebSocket | 内置 | 仪表盘增量数据推送 |
| 前端可视化 | D3.js | v7 | 内嵌于模板，无需 CDN |
| 数据持久化 | SQLite3 | 标准库 | 零配置，单文件数据库 |
| Windows API | pywin32 | >=306 | IFileOperation（回收站）、权限令牌、Shell 操作 |
| 进程管理 | psutil | >=5.9.0 | 进程状态检测、资源监控 |
| 配置解析 | PyYAML | >=6.0 | 规则引擎的 YAML 配置加载 |
| 文件锁 | filelock | >=3.12.0 | 跨平台单例锁 + JSON 原子写入 |

### 3.2 依赖管理（`requirements.txt`）

```
pywin32>=306
websockets>=12.0
psutil>=5.9.0
pyyaml>=6.0
fastapi>=0.100.0
uvicorn>=0.23.0
filelock>=3.12.0
```

### 3.3 运行环境要求

- Windows 10/11（x64）
- Python 3.10+ 已安装并加入 `PATH`
- 允许本地回环网络（127.0.0.1）通信
- 浏览器（Chrome/Edge/Firefox）


## 4. 通信协议选型与设计（核心决策）

### 4.1 问题定义

Agent 与本地 Python 后端之间的通信方式，是整个系统能够运转的**命脉**。方案书必须明确定义这条路径。

### 4.2 备选方案对比

| 方案 | 描述 | 优点 | 缺点 |
| :--- | :--- | :--- | :--- |
| **A：脚本执行 + HTTP（选用）** | Agent 执行 `api_client.py` 脚本，脚本通过 HTTP 与后端通信 | 适配所有 Agent 平台；实现简单；调试方便；Token 效率高 | Agent 需要支持执行本地脚本 |
| **B：MCP 协议（原生）** | 后端实现 MCP 服务器（JSON-RPC over stdio），Agent 原生调用 | Claude Desktop 原生集成 | 仅限 MCP 平台；实现复杂；调试困难 |
| **C：直接嵌入 Python 模块** | Agent 直接 `import disk_sense` 并调用函数 | 无网络开销 | Agent 通常无法直接导入本地模块 |

### 4.3 最终决策：采用方案 A

**核心理由**：

1. **通用性**：适用于所有支持 `Python` 工具的 Agent 平台（Cline、OpenAI Assistant、自定义 Agent）
2. **实现简单**：不需要学习 MCP 协议规范，只需标准 HTTP + Python 脚本
3. **调试方便**：每个脚本可独立测试（`python api_client.py start_scan --drive C:`）
4. **与仪表盘天然协同**：HTTP 后端同时服务 Agent 和浏览器
5. **Token 效率最高**：Agent 调用脚本时只需要传递必要参数，返回 JSON 直接进入上下文

### 4.4 通信流程

```
Agent 需要调用工具
    ↓
Agent 构造命令：python scripts/api_client.py {tool} {args}
    ↓
Agent 执行命令（subprocess / Bash）
    ↓
api_client.py 检查 localhost:58901/health
    ├── 存活 → 直接发送 HTTP 请求
    └── 未存活 → 调用 launcher.py --start 拉起服务
    ↓
后端处理 HTTP 请求
    ↓
api_client.py 将 HTTP 响应（JSON）打印到 stdout
    ↓
Agent 读取 stdout，获得结果（指纹档案 / 操作反馈）
    ↓
Agent 继续推理
```


## 5. 便携启动与生命周期管理

### 5.1 单例锁机制（原子文件锁）

**采用 `filelock` 原子锁，彻底消除 Check-Then-Act 竞态窗口**：

```python
from filelock import FileLock
import os

DATA_DIR = os.path.join(os.path.dirname(__file__), "../Data")
LOCK_FILE = os.path.join(DATA_DIR, "disk_sense.lock")

def acquire_singleton():
    """使用文件锁实现原子单例，无竞态窗口"""
    global lock_handle
    lock_handle = FileLock(LOCK_FILE, timeout=0.5)
    try:
        lock_handle.acquire(timeout=0.1)  # 非阻塞尝试获取
        return True  # 获取成功，当前进程为主进程
    except Timeout:
        return False  # 已有其他进程持有锁
```

### 5.2 按需寄生启动（On-Demand Parasitic Launch）

Agent 调用 `scripts/api_client.py` 时，该脚本自动执行以下逻辑：

1. **端口健康检查**：尝试连接 `http://localhost:58901/health`（超时 1 秒）
   - 若返回 200 → 服务已运行，直接使用
   - 若连接失败 → 执行下一步

2. **获取单例锁**：调用 `acquire_singleton()`
   - 若获取失败 → 已有服务进程持有锁，等待健康检查通过
   - 若获取成功 → 执行下一步

3. **拉起服务**：通过 `subprocess.Popen` 启动 `python -m disk_sense.server`
   - 使用 `CREATE_NO_WINDOW` 标志，避免弹出黑框
   - 使用 `DETACHED_PROCESS`，使其独立于 Agent 进程运行

4. **等待就绪**：最多轮询 15 次（每次 200ms），确认服务可用

### 5.3 空闲自毁（Idle Shutdown）

- 服务内置计时器，监控最后一条 WebSocket 消息和最后一次 HTTP API 调用的时间。
- **关键条件**：若 `scanner.is_running() == True`（扫描任务进行中），计时器**永不触发**自毁，防止缓存损坏。
- 若两者均超过 **300 秒（5 分钟）** 无活动，且无扫描任务运行，进程执行 `sys.exit(0)` 优雅退出。
- `Data/` 目录下的所有缓存保留，下次启动时复用。

### 5.4 启动脚本完整代码（`scripts/launcher.py`）

```python
import subprocess
import sys
import time
import requests
import os

SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
SERVER_MODULE = os.path.join(SCRIPT_DIR, "../disk_sense/server.py")
DATA_DIR = os.path.join(os.path.dirname(SCRIPT_DIR), "Data")
LOCK_FILE = os.path.join(DATA_DIR, "disk_sense.lock")
PORT = 58901
HEALTH_URL = f"http://localhost:{PORT}/health"

# 确保 Data 目录存在
os.makedirs(DATA_DIR, exist_ok=True)

def is_running():
    try:
        return requests.get(HEALTH_URL, timeout=1).status_code == 200
    except:
        return False

def acquire_singleton():
    """使用文件锁实现原子单例锁"""
    try:
        from filelock import FileLock, Timeout
        global lock_handle
        lock_handle = FileLock(LOCK_FILE, timeout=0.5)
        lock_handle.acquire(timeout=0.1)
        return True
    except (ImportError, Timeout):
        return False

def start_daemon():
    if is_running():
        print("✅ DiskSense 服务已在运行。")
        return True

    # 尝试获取单例锁，防止竞态
    if not acquire_singleton():
        # 锁已被占用，说明其他进程正在启动或已运行
        print("⏳ 其他进程正在启动服务，等待...")
        for _ in range(15):
            time.sleep(0.2)
            if is_running():
                print("✅ DiskSense 服务已启动。")
                return True
        print("❌ 等待超时。")
        return False

    print("🚀 正在启动 DiskSense 服务...")
    subprocess.Popen(
        [sys.executable, SERVER_MODULE],
        creationflags=subprocess.CREATE_NO_WINDOW | subprocess.DETACHED_PROCESS,
        stdout=subprocess.DEVNULL,
        stderr=subprocess.DEVNULL
    )

    for _ in range(15):
        time.sleep(0.2)
        if is_running():
            print("✅ DiskSense 服务启动成功，监听端口 58901。")
            return True

    print("❌ 启动超时，请手动检查 Python 环境。")
    return False

if __name__ == "__main__":
    if sys.argv[1] == "--start":
        sys.exit(0 if start_daemon() else 1)
    elif sys.argv[1] == "--stop":
        try:
            requests.get(f"http://localhost:{PORT}/shutdown", timeout=1)
        except:
            pass
        sys.exit(0)
```

### 5.5 HTTP 客户端脚本（`scripts/api_client.py`）

```python
import requests
import sys
import json
import argparse
import subprocess
import os
import time

BASE_URL = "http://localhost:58901"
SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))

def ensure_service():
    """确保服务已启动，若未启动则拉起"""
    try:
        resp = requests.get(f"{BASE_URL}/health", timeout=1)
        if resp.status_code == 200:
            return True
    except:
        pass

    # 服务未启动，调用 launcher.py 拉起
    subprocess.run([sys.executable, os.path.join(SCRIPT_DIR, "launcher.py"), "--start"])
    return True

def call_api_with_polling(endpoint, data=None, timeout=180):
    """调用 /scan 端点，若返回 scanning 则内部轮询直到完成"""
    ensure_service()

    # 先尝试调用 /scan
    resp = requests.post(f"{BASE_URL}/{endpoint}", json=data, timeout=120)
    result = resp.json()

    # 若扫描已完成，直接返回
    if result.get("status") == "completed":
        return result

    # 若返回 scanning，进入内部轮询
    if result.get("status") == "scanning":
        session_id = result.get("session_id")
        start_time = time.time()
        while time.time() - start_time < timeout:
            time.sleep(3)  # 每 3 秒轮询一次
            poll_resp = requests.get(
                f"{BASE_URL}/result",
                params={"session_id": session_id},
                timeout=10
            )
            poll_result = poll_resp.json()
            if poll_result.get("status") == "completed":
                return poll_result
            # 显示进度（stderr 不会被 Agent 捕获为输出）
            if "progress" in poll_result:
                print(f"⏳ 扫描进度: {poll_result['progress']*100:.1f}%", file=sys.stderr)

        # 超时后返回当前状态
        return {
            "status": "timeout",
            "session_id": session_id,
            "message": "扫描超过 180 秒仍未完成，请稍后手动查询"
        }

    return result

def call_api(endpoint, method="POST", data=None, params=None, timeout=60):
    """通用的 API 调用（非扫描端点）"""
    ensure_service()
    if method == "POST":
        resp = requests.post(f"{BASE_URL}/{endpoint}", json=data, timeout=timeout)
    else:
        resp = requests.get(f"{BASE_URL}/{endpoint}", params=params, timeout=timeout)
    return resp.json()

def main():
    parser = argparse.ArgumentParser(description="DiskSense API 客户端")
    parser.add_argument("tool", choices=[
        "start_scan", "query_detail", "classify_unknown",
        "viz_command", "execute_operation", "list_recent_ops",
        "undo_operation", "add_protection", "apply_tag"
    ])
    parser.add_argument("--drive", type=str)
    parser.add_argument("--entity_id", type=str)
    parser.add_argument("--category", type=str)
    parser.add_argument("--path", type=str)
    parser.add_argument("--action", type=str)
    parser.add_argument("--target", type=str)
    parser.add_argument("--payload", type=str)
    parser.add_argument("--op_type", type=str)
    parser.add_argument("--sources", type=str)
    parser.add_argument("--dest", type=str)
    parser.add_argument("--limit", type=int, default=10)
    parser.add_argument("--op_id", type=int)
    parser.add_argument("--tag", type=str)
    parser.add_argument("--timeout", type=int, default=180)
    args = parser.parse_args()

    result = None

    if args.tool == "start_scan":
        result = call_api_with_polling("scan", data={"drive": args.drive}, timeout=args.timeout)

    elif args.tool == "query_detail":
        result = call_api("detail", "GET", params={
            "entity_id": args.entity_id,
            "category": args.category
        })

    elif args.tool == "classify_unknown":
        result = call_api("classify", data={"path": args.path})

    elif args.tool == "viz_command":
        target = json.loads(args.target) if args.target else {}
        payload = json.loads(args.payload) if args.payload else {}
        result = call_api("viz", data={
            "action": args.action,
            "target": target,
            "payload": payload
        })

    elif args.tool == "execute_operation":
        sources = json.loads(args.sources) if args.sources else []
        result = call_api("operation", data={
            "op_type": args.op_type,
            "sources": sources,
            "dest": args.dest
        }, timeout=args.timeout)

    elif args.tool == "list_recent_ops":
        result = call_api("history", "GET", params={"limit": args.limit})

    elif args.tool == "undo_operation":
        result = call_api("undo", data={"op_id": args.op_id})

    elif args.tool == "add_protection":
        result = call_api("protect", data={"path": args.path, "add": True})

    elif args.tool == "apply_tag":
        result = call_api("tag", data={"path": args.path, "tag": args.tag})

    # 输出 JSON 结果供 Agent 读取
    print(json.dumps(result, ensure_ascii=False))

if __name__ == "__main__":
    main()
```


## 6. 极速扫描引擎（权限分层方案）

### 6.1 扫描策略

- **主路径（NTFS 本地硬盘）**：优先通过 `ctypes` 调用 `FSCTL_GET_NTFS_VOLUME_DATA` 或直接解析 `$MFT`（主文件表）读取文件元数据。
- **降级路径（网络盘 / 移动盘 / MFT 失败时）**：使用多线程生产者-消费者模式 + `os.walk`，工作线程数 = `max(1, CPU核心数 - 2)`。

### 6.2 主动盘符类型探测（避免先报错再降级）

```python
import ctypes

def get_drive_type(drive_letter):
    """获取盘符类型：3=本地硬盘, 4=网络盘, 2=移动盘, 5=CD-ROM"""
    return ctypes.windll.kernel32.GetDriveTypeW(f"{drive_letter}:\\")

def scan_drive(drive):
    drive_type = get_drive_type(drive)
    if drive_type == 3:  # DRIVE_FIXED（本地硬盘）
        try:
            return scan_via_mft(drive)
        except (PermissionError, OSError, NotImplementedError) as e:
            # 静默降级到 os.walk
            return scan_via_walk(drive)
    else:
        # 网络盘/移动盘/CD-ROM 直接走 os.walk
        return scan_via_walk(drive)
```

### 6.3 权限处理三层方案

| 层级 | 策略 | 适用场景 |
| :--- | :--- | :--- |
| **第一层：提权** | 启用 `SeBackupPrivilege` 令牌，使进程获得绕过部分 ACL 读取元数据的权限 | 系统目录（Windows、Program Files） |
| **第二层：静默降级** | 捕获 `PermissionError`，跳过该目录的子遍历，但保留该目录本身的大小（通过父级元数据估算），在报告中标记为“受限区域” | `System Volume Information`、其他用户的 `AppData` |
| **第三层：VSS 兜底** | 对极个别被独占锁定的文件（如 `hiberfil.sys`），通过 WMI 查询 Volume Shadow Copy 快照路径尝试读取元数据 | 系统休眠文件、注册表 Hive |
| **绝对禁止** | **永不执行** `takeown`（夺取所有权）或 `icacls`（修改 ACL） | 所有场景 |

### 6.4 死循环防护

- 维护 `visited_inode_set`（通过 `os.lstat` 获取 `st_ino`），跳过已访问的软链接/挂载点（Junction Points），如 `C:\Documents and Settings` → `C:\Users`。

### 6.5 稀疏文件处理

读取魔数前先判断实际占用大小，避免触发 Windows 的稀疏文件填充行为：

```python
def classify_magic_number(path):
    try:
        stat_info = os.stat(path)
        actual_size = stat_info.st_size
        if actual_size < 16:
            return {"magic_type": "EMPTY_OR_SPARSE", "confidence": "low"}
        with open(path, "rb") as f:
            header = f.read(16)
        # 识别魔数...
    except OSError:
        return {"magic_type": "UNKNOWN", "error": "无法读取"}
```


## 7. 指纹聚合器（Token 优化的核心）

### 7.1 设计目标

不将百万条文件路径直接传给 LLM（会导致 Token 爆炸），而是聚合成 **50～200 个“软件实体”**，使 JSON 指纹档案 ≤ 5000 Token。

### 7.2 聚合算法（贪婪匹配）

1. **提取种子名称**：从 `C:\Program Files`、`C:\Program Files (x86)`、`%APPDATA%`、`%LOCALAPPDATA%` 的一级子目录名中提取候选（如 `Google`、`Adobe`）

2. **角色映射（路径分类）**：
   - `program_base`：匹配 `Program Files`、`Program Files (x86)`
   - `user_data`：匹配 `AppData\Roaming`、`AppData\Local`（不含 `Temp`、`Cache` 子目录）
   - `cache`：匹配 `AppData\Local\Temp`、`AppData\Local\Cache`、`\Temp\`、`\Cache\`
   - `logs`：匹配 `\Logs\`、`\log\`、`_logs`

3. **关联扩展**：若某路径包含候选软件名（如 `.../Adobe/...`），但不在标准目录中（如 `C:\AdobeTemp`），也归入该实体，并标记 `location_anomaly: true`

4. **未归类文件**：无法匹配任何实体的文件（如根目录下的 `.iso`、`.zip`）放入 `global_anomalies` 列表，**聚合器自动批量调用魔数识别**，将 `magic_type` 直接填入每个条目，Agent 无需逐个查询。

### 7.3 输出 JSON 结构（指纹档案）

```json
{
  "session_id": "scan-20260820-143022",
  "drive": "C:",
  "entities": [
    {
      "id": "wechat",
      "total_size_mb": 4250,
      "locations": {
        "program_base": { "size_mb": 120, "file_count": 180, "has_exe": true },
        "user_data": { "size_mb": 1800, "file_count": 3200 },
        "cache": { "size_mb": 2330, "file_count": 450 }
      },
      "signals": ["CACHE_DOMINANT", "HAS_UNINSTALLER"],
      "last_access_days": 45,
      "top_extensions": [".tmp", ".log", ".dat"],
      "tags": ["chat", "tencent"]
    }
  ],
  "global_anomalies": [
    { "path_preview": "C:\\unknown_5GB.iso", "size_mb": 5120, "magic_type": "ISO" }
  ],
  "summary": {
    "total_scanned_mb": 256000,
    "skipped_dirs_count": 2,
    "skipped_paths": ["System Volume Information", "OtherUser\\AppData"],
    "scan_time_sec": 12.3
  }
}
```

**Token 估算**：200 实体 × ~220 字符 ≈ **4400 Token**，加上工具调用的往返，单次完整分析 ≤ 8000 Token。


## 8. 语义信号生成（非硬编码规则引擎）

### 8.1 设计哲学

代码中**绝不出现** `if "微信" in path` 或 `if "旧缓存" in message`。所有“聪明话”都由 Agent 基于客观信号即时生成。

### 8.2 结构化规则配置（`config/classification_rules.yaml`）

**采用结构化 JSON 逻辑 + Python 内置 operator 模块的安全评估**，而非危险的 `eval()`。

```yaml
rules:
  - signal: CACHE_DOMINANT
    condition:
      left: "locations.cache.size_mb"
      op: "gt"
      right:
        type: "expression"
        left: "locations.program_base.size_mb"
        op: "add"
        right: "locations.user_data.size_mb"
        multiplier: 1.5
    description: "缓存大小超过主程序与用户数据之和的1.5倍"

  - signal: EXE_MISSING
    condition:
      left: "locations.program_base.has_exe"
      op: "eq"
      right: false
      and:
        left: "locations.program_base.size_mb"
        op: "lt"
        right: 50
    description: "安装目录无exe且小于50MB，极大概率卸载残留"

  - signal: ANCIENT_DATA
    condition:
      left: "last_access_days"
      op: "gt"
      right: 180
      and:
        left: "total_size_mb"
        op: "gt"
        right: 1000
    description: "超过180天未访问且大于1GB"
```

### 8.3 安全评估器实现（`rules_engine.py`）

```python
import operator

class SafeEvaluator:
    """安全的条件评估器，支持嵌套路径取值和基本算术运算"""

    OPS = {
        "gt": operator.gt,
        "lt": operator.lt,
        "eq": operator.eq,
        "gte": operator.ge,
        "lte": operator.le,
        "add": operator.add,
        "sub": operator.sub,
        "mul": operator.mul,
        "truediv": operator.truediv,
        "and": lambda a, b: a and b,
        "or": lambda a, b: a or b,
    }

    def __init__(self, entity_data):
        self.entity = entity_data

    def _get_value(self, path: str):
        """从 entity 中按点号路径取值"""
        parts = path.split(".")
        value = self.entity
        for part in parts:
            if isinstance(value, dict):
                value = value.get(part)
            else:
                value = getattr(value, part, None)
            if value is None:
                return None
        return value

    def _evaluate_node(self, node):
        """递归评估条件节点"""
        if isinstance(node, (int, float, bool)):
            return node
        if isinstance(node, str):
            return self._get_value(node)
        if isinstance(node, dict):
            # 处理逻辑组合
            if "and" in node:
                return self._evaluate_node(node["and"])
            if "or" in node:
                return self._evaluate_node(node["or"])

            op = node.get("op")
            if op in ("add", "sub", "mul", "truediv"):
                left = self._evaluate_node(node.get("left"))
                right = self._evaluate_node(node.get("right"))
                multiplier = node.get("multiplier", 1.0)
                result = self.OPS[op](left, right)
                return result * multiplier
            elif op in ("gt", "lt", "eq", "gte", "lte"):
                left = self._evaluate_node(node.get("left"))
                right = self._evaluate_node(node.get("right"))
                return self.OPS[op](left, right)
        raise ValueError(f"无法评估的节点: {node}")

    def evaluate(self, condition_node):
        return self._evaluate_node(condition_node)
```

### 8.4 Agent 的推理转化

Agent 收到 `signals: ["CACHE_DOMINANT", "EXE_MISSING"]` 后，结合 `last_access_days`，动态生成自然语言：

> *“该实体（微信）的安装目录无主程序（EXE_MISSING），且缓存体积（2.3GB）远超主程序（120MB）。这表明软件虽已卸载，但残留了大量聊天缓存，距最后访问已过45天，建议清理。”*

**全程无硬编码。**


## 9. 实时仪表盘与可视化交互

### 9.1 生成策略

- `templates/template.html` 内嵌 D3.js（v7）、WebSocket 客户端和所有交互 JS 代码。仅留唯一占位符 `{{ TREEMAP_DATA }}`。
- 扫描完成后，脚本将聚合后的树形数据通过 `json.dumps` 填充占位符，生成完整独立的 HTML（无外部 CDN 依赖，可离线打开）。
- 静态快照保存至 `Data/reports/report_{timestamp}.html`。

### 9.2 实时动态更新

- **架构**：`disk_sense/server.py` 基于 FastAPI WebSocket 实现实时推送。
- **推送策略**：扫描线程每遍历完一个深度层级或每累积 200 个新节点，即通过 WebSocket 推送增量数据。
- **前端渲染**：D3.js 使用 `.data()` 更新 + `.transition().duration(300)` 动画，模拟 SpaceSniffer 的“生长”效果。
- **降级方案**：若 WebSocket 不可用，前端自动切换为轮询模式（`GET /poll?since=xxx`，每 500ms 一次）。

### 9.3 Agent 叠加层

前端维护独立的 SVG 覆盖层（Overlay），通过 WebSocket 接收 Agent 的 `viz_command` 指令（由 `api_client.py` 通过 HTTP 转发）：

| 动作 | 效果 | 示例参数 |
| :--- | :--- | :--- |
| `highlight` | 为指定方块增加霓虹色边框 + 发光动画 | `{"action":"highlight","target":{"id":"wechat"},"payload":{"color":"#FF4500","label":"卸载残留","effect":"pulse"}}` |
| `label` | 在方块右下角浮动显示短文本标签 | `{"action":"label","target":{"id":"adobe"},"payload":{"text":"⚠️ 缓存 2.3GB"}}` |
| `group` | 用虚线连接多个散落方块，表示“同一软件关联” | `{"action":"group","target":{"ids":["adobe_pf","adobe_appdata"]},"payload":{"color":"#00FFCC"}}` |
| `protect` | 将方块变为灰色锁定图标，禁用右键删除菜单 | `{"action":"protect","target":{"path":"D:/Work"}}` |
| `clear` | 清空所有叠加层 | `{"action":"clear"}` |

**Agent 零前端代码**，只传参数，前端负责渲染。

### 9.4 右键操作菜单

在 Treemap 方块上**右键**弹出原生风格菜单：
- 打开所在目录（`explorer /select,`）
- 复制到...（弹出文件夹选择器）
- 移动到...（弹出文件夹选择器）
- 压缩为 ZIP（调用后端压缩线程）
- 删除（走回收站）
- 查看属性（大小、路径、修改时间）

所有操作通过 WebSocket 发送至后端执行，自动记录 SQLite 日志。


## 10. Agent Skill 完整定义（大脑皮层）

### 10.1 核心文件：`SKILL.md`

```markdown
---
name: disk-sense-manager
description: 便携式 AI 磁盘文件管理器。用于分析磁盘空间、清理残留、迁移文件、查找重复项或回溯操作。
allowed-tools: Bash, Python, Read, Write
---

# DiskSense 便携磁盘管理器

## 1. 核心身份与铁律

你是 DiskSense，一个拥有本地磁盘"上帝视角"但极度尊重隐私的管家。

- **铁律 1（隐私）**：你**绝不**尝试读取或解析文件内容（文本/图片/Office）。你只能接触文件路径、大小、时间和文件头魔数（Magic Number）。这是最高优先级，不可违背。

- **铁律 2（安全）**：所有删除操作必须通过 `execute_operation` 工具，底层强制走回收站。执行批量移动（>10个文件或>1GB）前，必须向用户请求确认。

- **铁律 3（便携）**：若本地服务未启动，`api_client.py` 会自动调用 `launcher.py --start` 拉起服务，你无需手动处理。

## 2. 工作流（必须按此顺序执行）

1. **扫描阶段**：用户指定盘符（如 `C:`）后，调用 `start_scan(drive)`。**该工具会等待扫描完成并返回完整指纹档案 JSON**。若扫描耗时较长，请告知用户"扫描已开始，请查看浏览器仪表盘"。

2. **分析阶段**：收到指纹档案 JSON 后：
   - 解读每个实体的 `signals`（如 `CACHE_DOMINANT`、`EXE_MISSING`）
   - 结合 `last_access_days` 推理异常模式
   - `global_anomalies` 中已包含 `magic_type` 字段，无需再调用 `classify_unknown`（该工具仅用于用户指定特定路径时的按需查询）

3. **高亮阶段**：每推理完一个可疑实体，立即调用 `viz_command` 让仪表盘同步闪烁标记，方便用户在可视化界面中定位。

4. **执行阶段**：向用户展示清理建议，获得用户确认后，调用 `execute_operation` 执行操作。执行后调用 `list_recent_ops(limit=1)` 确认日志落盘。

5. **回溯阶段**：若用户要求撤销操作，调用 `undo_operation(op_id)` 执行五步预检并还原文件。

## 3. 工具函数（通过 `scripts/api_client.py` 调用）

> **说明**：以下所有工具均通过执行 Python 脚本实现。Agent 需构造命令：`python scripts/api_client.py {tool} {args}`，脚本会将结果（JSON）打印到 stdout，Agent 读取后继续推理。

### 3.1 扫描与查询

- **`start_scan(drive)`**
  - 命令：`python scripts/api_client.py start_scan --drive C:`
  - 功能：启动磁盘扫描，**同步等待扫描完成**（脚本内部自动轮询），返回完整指纹档案 JSON。
  - 返回：`{"status": "completed", "session_id": "xxx", "result": {"entities": [...], "global_anomalies": [...], "summary": {...}}}`

- **`query_detail(entity_id, category)`**
  - 命令：`python scripts/api_client.py query_detail --entity_id wechat --category cache`
  - 功能：获取特定实体在 `cache`/`logs`/`program_base` 下的 Top 5 文件路径及大小。
  - 返回：`[{"name": "1.log", "size": 200, "mtime": 123456}, ...]`

- **`classify_unknown(path)`**
  - 命令：`python scripts/api_client.py classify_unknown --path "C:\\unknown.iso"`
  - 功能：读取文件头魔数，返回真实格式（仅用于特定路径的按需查询）。
  - 返回：`{"magic_type": "ISO 9660", "mime": "application/x-iso"}`

### 3.2 可视化操控

- **`viz_command(action, target, payload)`**
  - 命令：`python scripts/api_client.py viz_command --action highlight --target '{"id":"wechat"}' --payload '{"color":"#FF0000","label":"缓存膨胀"}'`
  - `action`：`highlight` | `label` | `group` | `protect` | `clear`
  - `target`：`{"id": "软件ID"}` 或 `{"path": "绝对路径"}`
  - `payload`：`{"color": "#FF0000", "label": "缓存膨胀", "effect": "pulse"}`

### 3.3 文件操作

- **`execute_operation(op_type, sources, dest)`**
  - 命令：`python scripts/api_client.py execute_operation --op_type move --sources '["C:\\a.txt"]' --dest "D:\\"`
  - `op_type`：`move` | `copy` | `delete` | `compress`
  - **删除自动走回收站**，绝不永久擦除。
  - 返回：`{"op_uuid": "abc-123", "status": "queued"}`

### 3.4 回滚与审计

- **`list_recent_ops(limit)`**
  - 命令：`python scripts/api_client.py list_recent_ops --limit 10`
  - 返回：操作记录列表（含 UUID、时间、源路径、状态）

- **`undo_operation(op_id)`**
  - 命令：`python scripts/api_client.py undo_operation --op_id 1`
  - 功能：执行五步预检（父目录存在性、冲突重命名、权限校验），若通过则物理还原文件。
  - 返回：`{"status": "success", "restored_to": "C:\\file_restored_1.txt"}` 或 `{"status": "failed", "error": "父目录不存在"}`

### 3.5 用户偏好

- **`add_protection(path)`**
  - 命令：`python scripts/api_client.py add_protection --path "D:/Work"`
  - 返回：`{"status": "added"}`

- **`apply_tag(path, tag)`**
  - 命令：`python scripts/api_client.py apply_tag --path "E:/Downloads" --tag "temp"`
  - 返回：`{"status": "tagged"}`

## 4. 输出格式要求

- **禁止**直接打印冗长的 JSON 原文给用户。
- 必须用自然语言总结。例如：
  - *"发现微信占用 4.2GB，其中缓存占 2.3GB（超过 1 年未清理），建议清理。已在高亮仪表盘中标红。"*
  - *"撤销失败：原始文件夹 'D:\\Work' 已被删除，无法还原。建议手动从回收站恢复。"*
```

### 10.2 SKILL.md 设计原则

| 设计点 | 理由 |
| :--- | :--- |
| **工具通过脚本调用** | 明确 Agent 与后端的通信方式，消除方案书中的协议断层 |
| **铁律写在最顶部** | 防止 LLM 在长上下文推理中“遗忘”隐私约束 |
| **脚本内置轮询** | Agent 只调用一次 `start_scan`，脚本内部完成扫描等待和进度轮询 |
| **工具粒度适中** | 不暴露 `os.listdir` 给 Agent，只暴露 `query_detail`（Top 5 摘要） |
| **可视化指令解耦** | `viz_command` 让 Agent 能“画画”但不必知道 SVG 语法 |


## 11. 本地服务 API 契约（通信协议的具体实现）

### 11.1 端点总览

| 端点 | 方法 | 入参 | 返回 | 对应工具 |
| :--- | :--- | :--- | :--- | :--- |
| `/scan` | POST | `{"drive": "C:"}` | `{"status":"completed","session_id":"xxx","result":{...}}` | `start_scan` |
| `/result` | GET | `?session_id=xxx` | `{"status":"scanning","progress":0.5}` 或 `{"status":"completed","result":{...}}` | 内部轮询 |
| `/detail` | GET | `?entity_id=wechat&category=cache` | `[{"name":"1.log","size":200,...}]`（Top 5） | `query_detail` |
| `/classify` | POST | `{"path": "C:\\x.iso"}` | `{"magic_type": "ISO 9660"}` | `classify_unknown` |
| `/viz` | POST | `{"action":"highlight","target":{"id":"wechat"},"payload":{...}}` | `{"status":"ok"}` | `viz_command` |
| `/operation` | POST | `{"op_type":"move","sources":["C:\\a.txt"],"dest":"D:\\"}` | `{"op_uuid":"abc-123"}` | `execute_operation` |
| `/history` | GET | `?limit=10` | `[{"id":1,"op_type":"delete","source":"C:\\x",...}]` | `list_recent_ops` |
| `/undo` | POST | `{"op_id": 1}` | `{"status":"undone"}` 或 `{"error":"父目录不存在"}` | `undo_operation` |
| `/protect` | POST | `{"path":"D:/Work","add":true}` | `{"status":"ok"}` | `add_protection` |
| `/tag` | POST | `{"path":"E:/Downloads","tag":"temp"}` | `{"status":"ok"}` | `apply_tag` |
| `/shutdown` | GET | 无 | 进程退出 | 自毁 |
| `/health` | GET | 无 | `{"status":"alive"}` | 启动检测 |

### 11.2 `/scan` 端点详细说明（核心）

```python
# POST /scan
# 入参：{"drive": "C:"}
# 行为：同步阻塞，等待扫描完全完成后再返回

# 返回（成功）：
{
  "status": "completed",
  "session_id": "scan-20260820-143022",
  "result": {
    "entities": [...],
    "global_anomalies": [...],
    "summary": {...}
  }
}

# 超时保护：若扫描超过 120 秒仍未完成，返回：
{
  "status": "scanning",
  "session_id": "scan-20260820-143022",
  "message": "扫描仍在进行中，请轮询 /result",
  "progress": 0.75
}
```


## 12. 文件操作与持久化回溯引擎

### 12.1 删除操作的“双重保险”

- **物理层**：调用 Windows `IFileOperation` 接口 + `FOF_ALLOWUNDO` 标志，确保文件进入回收站，而非永久删除。绝不使用 `os.remove`。
- **逻辑层**：执行前在 SQLite 插入一条 `DELETE` 记录，保存源路径、文件大小、修改时间。

### 12.2 回收站映射的精确捕获

**删除时捕获系统分配的 `$R` 文件名**，避免事后匹配的误差和性能开销：

```python
def delete_to_recycle_bin_with_mapping(source_path):
    """
    删除文件并捕获回收站映射信息。
    通过对比删除前后回收站文件列表，获取新增的 $Rxxxxxx 名称。
    """
    shell_app = win32com.client.Dispatch("Shell.Application")
    recycle_bin = shell_app.NameSpace(0x0a)  # shell:RecycleBinFolder

    # 删除前记录回收站文件列表
    before_items = set()
    for item in recycle_bin.Items():
        before_items.add(item.Name)

    # 执行删除操作
    # ... IFileOperation::DeleteItem ...

    # 删除后对比，新增的 $Rxxxxxx 即为本次删除的映射
    after_items = set()
    for item in recycle_bin.Items():
        after_items.add(item.Name)
    new_items = after_items - before_items

    # 通过大小和时间戳匹配（此时只匹配新增的几个）
    for item in recycle_bin.Items():
        if item.Name in new_items:
            if size_match and time_match:
                return {
                    "recycle_bin_name": item.Name,      # $RXXXXXX
                    "recycle_info_name": item.Name.replace("$R", "$I")
                }
```

### 12.3 SQLite 日志表结构（`Data/op_log.db`）

```sql
CREATE TABLE operation_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    op_uuid TEXT UNIQUE NOT NULL,
    session_id TEXT,
    op_type TEXT NOT NULL,
    source_path TEXT NOT NULL,
    dest_path TEXT,
    file_size INTEGER,
    file_mtime REAL,
    recycle_bin_name TEXT,
    recycle_info_name TEXT,
    status TEXT DEFAULT 'ACTIVE',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    error_msg TEXT
);

CREATE INDEX idx_op_created ON operation_log (created_at DESC);
CREATE INDEX idx_op_uuid ON operation_log (op_uuid);
```

### 12.4 五步回滚预检协议

```python
def execute_undo(op_id):
    record = db.fetch(op_id)

    # 步骤 1：状态锁定
    if record.status == 'UNDONE':
        return {"status": "skipped", "message": "此操作已撤销过"}

    # 步骤 2：父目录存活检测
    parent_dir = os.path.dirname(record.source_path)
    if not os.path.exists(parent_dir):
        db.update_status(op_id, 'FAILED', error="父目录已删除")
        return {"status": "failed", "error": f"父目录 '{parent_dir}' 不存在"}

    # 步骤 3：冲突重命名策略（防覆盖）
    target_path = record.source_path
    if os.path.exists(target_path):
        base, ext = os.path.splitext(target_path)
        counter = 1
        while os.path.exists(f"{base}_restored_{counter}{ext}"):
            counter += 1
        target_path = f"{base}_restored_{counter}{ext}"

    # 步骤 4：权限预检
    if not os.access(parent_dir, os.W_OK):
        db.update_status(op_id, 'FAILED', error="无写入权限")
        return {"status": "failed", "error": f"无权限写入 '{parent_dir}'"}

    # 步骤 5：执行物理回滚
    if record.op_type == 'DELETE':
        # 优先使用记录中的 recycle_bin_name 精确还原
        if record.recycle_bin_name:
            result = restore_from_recycle_bin_by_name(
                record.recycle_bin_name, target_path
            )
        else:
            # 降级：使用 SHFileOperation 的 FO_MOVE
            result = undo_delete_via_shfileoperation(record.source_path)
    elif record.op_type in ('MOVE', 'RENAME'):
        shutil.move(record.dest_path, target_path)

    db.update_status(op_id, 'UNDONE')
    return {"status": "success", "restored_to": target_path}
```

### 12.5 批量操作原子性

- Agent 调用 `execute_operation` 传入多个 `sources`，后端生成一个 `op_uuid`，所有条目写入同一批次。
- 回滚时，读取该 `op_uuid` 下所有记录，逐条执行。若某条失败，跳过并继续处理其余，最终汇总报告。

### 12.6 日志修剪策略

- 默认保留 **最近 30 天** 的操作记录。
- 超期记录在服务空闲时自动转存为 `Data/archive/op_log_YYYYMMDD.json.gz`，并从 SQLite 中清除。


## 13. 用户偏好与长期记忆系统

Agent 不能每次都“失忆”。用户可能说：“以后别动我的 `D:/Work` 文件夹。”或“给 `E:/Downloads` 打上 `temp` 标签。”

### 13.1 持久化存储（`Data/user_preferences.json`）

```json
{
  "protected_paths": ["D:/Work", "C:/Program Files"],
  "tags": {
    "D:/Projects/Alpha": "work",
    "E:/Downloads": "temp"
  },
  "ignore_patterns": ["*.pdb", "*.tmp", "Thumbs.db"],
  "auto_clean_rules": {
    "temp": { "max_age_days": 30, "enabled": true },
    "logs": { "max_age_days": 90, "enabled": false }
  }
}
```

### 13.2 原子写入策略（防止并发崩溃）

```python
from filelock import FileLock

def atomic_write_json(data, filepath):
    """原子写入 JSON 文件"""
    # 先写临时文件
    temp_path = filepath + ".tmp"
    with open(temp_path, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
        f.flush()
        os.fsync(f.fileno())

    # 原子替换（Windows 和 POSIX 均支持）
    os.replace(temp_path, filepath)

def safe_write_preferences(data):
    lock_path = os.path.join(DATA_DIR, "user_preferences.json.lock")
    with FileLock(lock_path):
        atomic_write_json(data, os.path.join(DATA_DIR, "user_preferences.json"))
```


## 14. 系统资源治理与性能优化

### 14.1 IO 与 CPU 优先级控制

- 扫描线程启动时调用 Windows API：
  - `SetPriorityClass(GetCurrentProcess(), BELOW_NORMAL_PRIORITY_CLASS)`
  - `SetThreadPriority(GetCurrentThread(), THREAD_MODE_BACKGROUND_BEGIN)`

### 14.2 线程数控制

- `config/config.yaml` 中允许用户配置 `max_workers: 4`
- 默认 = `max(1, CPU核心数 - 2)`

### 14.3 异步事件循环下的并发模型

**关键**：所有同步阻塞操作必须委派到线程池，防止 WebSocket 断连：

```python
@app.post("/scan")
async def start_scan(request):
    # 在独立线程中执行同步扫描，不阻塞事件循环
    scanner = await asyncio.to_thread(scan_drive_sync, drive)
    return {"status": "completed", "result": scanner.to_dict()}

@app.websocket("/ws")
async def websocket_endpoint(websocket: WebSocket):
    await websocket.accept()
    while True:
        # 同样使用 to_thread 获取进度
        progress = await asyncio.to_thread(scanner.get_progress)
        await websocket.send_json({"progress": progress})
        await asyncio.sleep(1)
```

### 14.4 主动节流

- 扫描循环每处理 **1000 个文件** 后，调用 `time.sleep(0.001)` 主动让出 CPU 时间片。

### 14.5 缓存加速

- 首次扫描完成后，将聚合结果存入 `Data/cache.db`
- 若用户在 24 小时内再次扫描同一盘符，且未检测到文件系统变更（通过 USN Journal 比对），直接返回缓存，响应时间 < 100ms。


## 15. 隐私与安全合规声明

| 检查项 | 实现状态 |
| :--- | :--- |
| 是否读取文件文本内容（PDF/Word/Excel/TXT）？ | **绝不读取** |
| 文件头魔数是否读取？ | 仅读取前 16 字节用于类型识别，不记录、不上传 |
| 稀疏文件处理？ | 先判断实际占用大小，避免触发稀疏文件填充 |
| 数据是否上传云端？ | 全部本地存储，零上传 |
| 删除是否可逆？ | 强制走回收站 + 精确 $R 映射 + SQLite 日志 |
| 权限操作是否越界？ | 禁用 `takeown` 和 `icacls` |
| Agent 操作边界？ | `execute_operation` 校验 `source_path` 是否属于本次扫描盘符 |


## 16. 部署结构与交付清单

### 16.1 最终交付物：Agent Skill 压缩包

```
disk-sense-skill.zip
│
├── SKILL.md                          # Agent 核心提示词（必须加载）
│
├── scripts/                          # Agent 可调用的胶水脚本
│   ├── launcher.py                   # 启动/停止服务（含 filelock 单例锁）
│   └── api_client.py                 # HTTP 客户端（内置扫描轮询逻辑）
│
├── disk_sense/                       # 核心引擎 Python 包
│   ├── __init__.py
│   ├── server.py                     # FastAPI + WebSocket（asyncio.to_thread）
│   ├── scanner.py                    # MFT/os.walk + 主动盘符类型探测
│   ├── aggregator.py                 # 指纹聚合 + 批量魔数识别
│   ├── rules_engine.py               # 结构化 YAML 条件安全评估
│   ├── file_operator.py              # IFileOperation + 回收站映射捕获
│   ├── undo_manager.py               # SQLite + 五步回滚
│   ├── preferences.py                # 原子写入 + filelock
│   └── templates/
│       └── template.html             # 仪表盘模板（含 D3.js）
│
├── config/
│   ├── classification_rules.yaml     # 结构化条件规则（非 eval）
│   └── config.yaml                   # 线程数、超时、日志保留天数
│
├── Data/                             # 运行时自动生成（首次启动创建）
│   ├── cache.db                      # 扫描缓存
│   ├── op_log.db                     # 操作日志（含回收站映射字段）
│   ├── user_preferences.json         # 用户标签/保护列表
│   ├── disk_sense.lock               # 单例文件锁
│   ├── reports/                      # 历史 HTML 报告
│   └── archive/                      # 超期日志压缩归档
│
├── requirements.txt                  # Python 依赖列表（完整）
└── README.md                         # 用户使用指南（含各平台配置说明）
```

### 16.2 部署步骤（用户侧）

**步骤 1：解压 Skill 包**
将 `disk-sense-skill.zip` 解压到任意目录，例如：
```
C:\Users\用户名\Documents\AgentSkills\disk-sense-skill\
```

**步骤 2：安装 Python 依赖**
打开终端，进入 Skill 目录，执行：
```bash
pip install -r requirements.txt
```

**步骤 3：配置 Agent 平台加载 SKILL.md**

**Cline（VSCode 插件）**：
- 将 `disk-sense-skill/` 文件夹放入 Cline 的 Skills 目录（如 `~/.cline/skills/`）
- 或在 Cline 设置中指定 Skill 文件夹路径
- Cline 会自动加载 `SKILL.md`，并允许 Agent 调用 `scripts/` 下的 Python 脚本

**OpenAI Assistant / 自定义 Agent**：
- 将 `SKILL.md` 的内容作为 System Prompt 的一部分
- 将 `scripts/` 下的脚本注册为可调用工具（通过 `tools` 参数）

**Claude Desktop（通过 MCP）**：
> 注：Claude Desktop 原生 MCP 不支持直接执行脚本。如需在此平台使用，需将 `api_client.py` 的逻辑封装为 MCP 工具。但**推荐使用 Cline 或 OpenAI Assistant** 以获得最佳体验。

**步骤 4：验证**
在聊天框输入“扫描 C 盘”，Agent 应自动调用 `api_client.py`，拉起服务并返回结果。

### 16.3 卸载方式
- 删除整个 `disk-sense-skill` 文件夹
- 如有 Python 依赖，可执行 `pip uninstall -r requirements.txt`（可选）


## 17. 完整用户旅程（端到端闭环）

```
┌─────────────────────────────────────────────────────────────────────┐
│ 用户： "帮我分析一下 C 盘，看看哪些可以清理"                      │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Agent 收到指令，调用 start_scan("C:")                             │
│ 执行：python scripts/api_client.py start_scan --drive C:          │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ api_client.py 检查 localhost:58901/health                         │
│ → 未启动，调用 launcher.py --start 拉起服务                       │
│ → launcher.py 使用 filelock 获取单例锁，防止竞态                  │
│ → 等待服务就绪，发送 POST /scan {"drive": "C:"}                  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 后端同步扫描（12 秒），通过 asyncio.to_thread 委派到线程池        │
│ → 主动探测盘符类型，本地硬盘走 MFT，网络盘走 os.walk              │
│ → 同时通过 WebSocket 推送增量数据到仪表盘                         │
│ → 浏览器中 Treemap 动态生长                                       │
│ → 扫描完成，生成完整指纹档案 JSON                                 │
│ → 返回 HTTP 响应：{"status":"completed","result":{...}}           │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ api_client.py 将 JSON 打印到 stdout                               │
│ Agent 读取 stdout，获得指纹档案                                   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Agent 分析 signals：                                               │
│ - wechat: CACHE_DOMINANT + EXE_MISSING → 卸载残留               │
│ - adobe: ANCIENT_DATA (last_access: 200天) → 归档建议            │
│ - global_anomalies 中已有 magic_type，无需逐个查询               │
│ 调用 viz_command 高亮仪表盘对应方块                              │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Agent 回复用户：                                                   │
│ "扫描完成！发现微信残留缓存 2.3GB（已卸载），Adobe 旧文件 1.8GB   │
│ （200天未访问）。已在仪表盘用红色高亮显示，请确认是否清理。"      │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 用户： "清理微信缓存"                                              │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Agent 调用 execute_operation(delete, sources=[...])              │
│ → 后端通过 IFileOperation 删除，同时捕获 $R 映射名               │
│ → 写入 SQLite 日志（含 recycle_bin_name 字段）                   │
│ → 返回 op_uuid                                                   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Agent 回复： "已清理 2.3GB，文件已移至回收站。如需恢复，请告知我。" │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ 用户： "撤销刚才的删除"                                            │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Agent 调用 list_recent_ops(limit=1) → 获取最近操作 ID            │
│ Agent 调用 undo_operation(op_id) → 五步预检                       │
│ → 使用记录中的 recycle_bin_name 精确还原                          │
│ → 若 $R 名缺失，降级为 SHFileOperation FO_MOVE                   │
│ Agent 回复："已恢复微信缓存至原位置。"                             │
└─────────────────────────────────────────────────────────────────────┘
```


## 18. 验收标准

| 指标 | 达标线 | 测试方法 |
| :--- | :--- | :--- |
| **扫描速度（1TB 机械硬盘）** | ≤ 45 秒 | 测试 3 次取平均值 |
| **扫描速度（500GB SSD）** | ≤ 15 秒 | 测试 3 次取平均值 |
| **Token 消耗（单次分析）** | ≤ 8000 Token | 监控 Agent 平台计费日志 |
| **指纹聚合实体数** | 50～200 个（C 盘典型安装） | 抽查 5 台不同电脑 |
| **回滚成功率（父目录存续）** | ≥ 99.9% | 执行 1000 次随机撤销操作 |
| **回收站映射准确率** | 100%（$R 名捕获后） | 验证删除前后回收站文件列表 |
| **误删防护** | 100% 经回收站，0% 永久丢失 | 审计 `op_log.db` 所有 DELETE 记录 |
| **单例锁准确率** | 100%（无双进程） | 并发启动 10 次测试 |
| **事件循环阻塞检测** | WebSocket 心跳 < 5% 丢包 | 长时扫描 + WS 连接稳定性测试 |
| **内存占用（空闲时）** | ≤ 80MB | Windows 任务管理器 |
| **CPU 占用（扫描时）** | ≤ 单核 70%（节流生效） | 性能监视器 |
| **便携性验证** | 删除整个文件夹后系统无残留 | 检查注册表 + 服务列表 |


## 19. 自主审查与改进记录

本方案书经过多轮自我审查与专业审阅，以下为关键修复记录：

| 轮次 | 发现的问题 | 改进措施 |
| :--- | :--- | :--- |
| **第 1 轮** | 方案书大量提及 `DiskSense.exe`，但技术栈实际是 Python | 删除所有 exe 表述，统一为 Python 脚本 |
| **第 2 轮** | 未定义 Agent 与后端的通信协议 | 新增第 4 章，明确采用方案 A（脚本执行 + HTTP） |
| **第 3 轮** | SKILL.md 中工具定义仅为函数签名，未说明如何调用 | 将工具定义改为可执行的脚本命令格式 |
| **第 4 轮** | 未说明 Agent 如何获取扫描结果（指纹档案） | 修改 `/scan` 为同步阻塞模式，脚本内置轮询逻辑 |
| **第 5 轮** | 缺少 api_client.py 和 launcher.py 的完整代码 | 在第 5 章补充完整脚本代码 |
| **第 6 轮** | YAML 规则引擎的字符串条件存在安全漏洞 | 改为结构化 JSON 逻辑 + 安全评估器 |
| **第 7 轮** | 回收站还原的 $R 映射未定义 | 删除时捕获 $R 名 + 降级 SHFileOperation |
| **第 8 轮** | PID 单例锁存在 Check-Then-Act 竞态窗口 | 改用 filelock 原子文件锁 |
| **第 9 轮** | FastAPI 同步阻塞会冻结事件循环 | 增加 asyncio.to_thread 并发模型说明 |
| **第 10 轮** | global_anomalies 批量处理策略未定义 | 聚合器自动批量识别魔数 |
| **第 11 轮** | 稀疏文件魔数读取会触发磁盘填充 | 先判断实际占用大小 |
| **第 12 轮** | filelock 未列入 requirements.txt | 补充 filelock>=3.12.0 |


## 最终交付声明

本方案书 v3.3 已完整融合全部历史对话中的所有需求与三轮审阅的全部修订：

| 需求类别 | 状态 |
| :--- | :--- |
| SpaceSniffer 风格的磁盘扫描 + 实时仪表盘 | ✅ |
| 权限处理三层方案 | ✅ |
| Agent 关联分析与指纹聚合（Token 优化） | ✅ |
| Agent 操控前端（viz_command 叠加层） | ✅ |
| 纯 Python 技术栈 + 便携无痕 | ✅ |
| 全量可回溯（SQLite + 五步回滚 + 回收站精确映射） | ✅ |
| 零内容读取（隐私铁律）+ 稀疏文件处理 | ✅ |
| 用户偏好与标签持久化 | ✅ |
| 资源治理（IO/CPU 优先级 + 异步并发模型） | ✅ |
| 明确的 Agent ↔ 后端通信协议（方案 A） | ✅ |
| 脚本内置扫描轮询（Agent 单次调用） | ✅ |
| 原子单例锁（filelock，消除竞态） | ✅ |
| 结构化 YAML 规则引擎（安全评估，非 eval） | ✅ |
| 完整的 api_client.py 和 launcher.py 代码 | ✅ |
| FastAPI + asyncio.to_thread 并发模型 | ✅ |
| 完整用户旅程闭环 | ✅ |
